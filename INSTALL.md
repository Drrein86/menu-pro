# 🚀 הוראות התקנה - MenuPro Local Display

## דרישות מקדימות

לפני ההתקנה, וודא שיש לך:
- **Node.js** גרסה 16 ומעלה ([הורד כאן](https://nodejs.org/))
- **npm** (מותקן עם Node.js)

## שלב 1: התקנת תלויות השרת

פתח טרמינל/PowerShell בתיקיית הפרויקט והרץ:

```bash
npm install
```

פקודה זו תתקין את כל התלויות הבאות:
- Express - שרת web
- SQLite - מסד נתונים
- Multer - העלאת קבצים
- CORS - תמיכה בבקשות cross-origin

## שלב 2: התקנת תלויות הקליינט

```bash
cd client
npm install
cd ..
```

פקודה זו תתקין:
- React - ספריית UI
- Vite - כלי build מהיר
- Tailwind CSS - framework עיצוב
- Axios - בקשות HTTP
- React Router - ניווט
- Lucide Icons - אייקונים

## שלב 3: הרצת המערכת

מתיקיית הבסיס (menu2), הרץ:

```bash
npm run dev
```

פקודה זו תפעיל **במקביל**:
- 🖥️ שרת Backend על `http://localhost:3000`
- 🎨 אפליקציית Frontend על `http://localhost:5173`

## שלב 4: פתיחת המערכת

לאחר ההפעלה, פתח בדפדפן:

### 📺 דף התצוגה (למסך הטלוויזיה/טאבלט)
```
http://localhost:5173
```

### ⚙️ פאנל הניהול
```
http://localhost:5173/admin
```

---

## 🎯 שימוש ראשוני

### 1. הוספת לוגו
- גש לפאנל הניהול → **לוגו**
- העלה תמונה או הזן URL
- לחץ **שמור לוגו**

### 2. הוספת וידאו רקע
- גש לפאנל הניהול → **וידאו**
- בחר בין העלאת קובץ מקומי או קישור YouTube
- לחץ **שמור וידאו**

### 3. עריכת מוצרים
- גש לפאנל הניהול → **מוצרים**
- לחץ **הוסף מוצר**
- מלא פרטים: שם, מחיר, תיאור, קטגוריה
- אפשר להעלות תמונה למוצר
- לחץ **שמור**

### 4. התאמת עיצוב
- גש לפאנל הניהול → **עיצוב**
- שנה צבעים: רקע, טקסט, הדגשה
- בחר גופן מתאים
- או בחר ערכת נושא מוכנה
- לחץ **שמור עיצוב**

---

## 🖥️ הצגה על טלוויזיה/טאבלט

### אופציה 1: חיבור ישיר
1. חבר את הטלוויזיה/טאבלט לאותה רשת של המחשב
2. גלה את כתובת ה-IP של המחשב:
   - Windows: `ipconfig` (חפש IPv4 Address)
   - Mac/Linux: `ifconfig` או `ip addr`
3. בטלוויזיה/טאבלט, פתח דפדפן וגש ל:
   ```
   http://[IP_ADDRESS]:5173
   ```
   לדוגמה: `http://192.168.1.100:5173`

### אופציה 2: מצב מסך מלא
- לחץ **F11** בדפדפן להצגה במסך מלא
- או לחץ באייקון המסך המלא בדפדפן

---

## 📦 הפעלה בסביבת ייצור (Production)

### 1. בניית הקליינט
```bash
cd client
npm run build
cd ..
```

### 2. הגשת קבצים סטטיים מהשרת
ערוך את `server/index.js` והוסף:
```javascript
app.use(express.static(join(__dirname, '..', 'client', 'dist')));
```

### 3. הפעלת השרת
```bash
npm start
```

---

## 🔧 פתרון בעיות נפוצות

### השרת לא עולה
- וודא ש-Node.js מותקן: `node --version`
- וודא שהפורט 3000 פנוי
- הרץ `npm install` שוב

### הקליינט לא עולה
- וודא שהתקנת את התלויות: `cd client && npm install`
- נקה cache: `cd client && rm -rf node_modules && npm install`
- וודא שהפורט 5173 פנוי

### תמונות/וידאו לא נטענים
- וודא שתיקיית `uploads/` קיימת
- בדוק הרשאות כתיבה לתיקייה
- וודא שהקבצים לא גדולים מדי (מקס 100MB)

### העיצוב לא משתנה
- רענן את הדפדפן (Ctrl+F5)
- נקה cache של הדפדפן
- וודא שלחצת על **שמור עיצוב**

---

## 📁 מבנה הפרויקט

```
menu2/
├── server/              # Backend
│   ├── index.js        # שרת Express
│   └── db.js           # מסד נתונים SQLite
├── client/             # Frontend
│   ├── src/
│   │   ├── pages/      # דפים (Display, Admin)
│   │   ├── components/ # קומפוננטות
│   │   └── api/        # קריאות API
│   └── package.json
├── uploads/            # קבצים שהועלו
├── menu.db            # מסד נתונים (נוצר אוטומטית)
└── package.json
```

---

## 🆘 תמיכה

נתקלת בבעיה? בדוק:
1. קובץ `README.md` למידע נוסף
2. לוגים של השרת בטרמינל
3. Console של הדפדפן (F12)

---

נוצר בגאווה עם ❤️

